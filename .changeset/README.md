# Changesets

Use `bun run changeset` to add a release note for the next publish.

When a changeset reaches `main`, the publish workflow opens or updates a release PR.

After the release PR is merged, the same workflow publishes the package.
